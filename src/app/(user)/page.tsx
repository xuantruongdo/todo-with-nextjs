const ProjectPage = () => {
  return (
    <div
      className="flex items-center justify-center h-screen"
      style={{ width: "calc(100vw - 255px)" }}
    >
      <h2 className="text-2xl font-bold text-center"> Choose a project</h2>
    </div>
  );
};

export default ProjectPage;
